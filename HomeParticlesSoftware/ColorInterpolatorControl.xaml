<UserControl x:Class="HomeParticlesSoftware.ColorInterpolatorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomeParticlesSoftware"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="#25265c" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <local:MinMaxRGBColorControl x:Name="ColorMin" Grid.Column="0" 
                                     HorizontalAlignment="Center"/>
        <local:MinMaxRGBColorControl x:Name="ColorMax" Grid.Column="1" 
                                     HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
